import { MajorityDirective } from './majority.directive';

describe('MajorityDirective', () => {
  it('should create an instance', () => {
    const directive = new MajorityDirective();
    expect(directive).toBeTruthy();
  });
});
